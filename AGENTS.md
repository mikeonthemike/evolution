# Evolution

## Purpose
This explorer is a framework to help thinking about how biological evolution does, and doesn't apply to AI.

## Structure
The explorer is designed to be HTML only

## To Do
- (All previous items completed — see Done.)

## Done
1. **Mobile responsiveness** — Sidebar becomes a horizontal scrollable tab strip below the header on screens under 768px; main content full width; header/legend stack on small screens.
2. **Meta/OG tags** — Title "Evolution × AI: A Pattern Library", description from preamble lead, `og-image.svg` placeholder colour card, `og:image:alt`.
3. **Accessibility** — Sidebar is `<nav>` with `aria-label`; items are `<button>`s with `aria-label` and `aria-current`; keyboard navigation (Arrow Up/Down/Left/Right); `:focus-visible` outline; `<main>` with `aria-label`.
4. **Polish** — Smooth scroll-to-top on entry change; subtle `.entry-inner` fade-in; system font fallbacks throughout.
5. **Fonts** — Google Fonts removed; system stack only (Georgia/Times, Consolas/Monaco/Menlo, system-ui/Segoe UI) for performance and privacy.